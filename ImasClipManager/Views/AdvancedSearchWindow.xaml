<Window x:Class="ImasClipManager.Views.AdvancedSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="高度な検索" 
        SizeToContent="Height" MaxHeight="800" Width="550"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#DDDDDD"/>
            <Setter Property="BorderBrush" Value="#707070"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EEEEEE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="border" Width="16" Height="16" CornerRadius="3" 
                                    BorderBrush="Gray" BorderThickness="1" Background="White" 
                                    Margin="0,0,5,0" VerticalAlignment="Center">
                                <Path x:Name="CheckMark" Data="M 3,8 L 6,11 L 13,4" 
                                      Stroke="Black" StrokeThickness="2" 
                                      Visibility="Collapsed" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>

        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Margin" Value="0,-10,0,15"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Style.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PlaceholderTextStyle" TargetType="TextBlock">
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="Foreground" Value="LightGray"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Visibility" Value="Collapsed"/>

            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=Children[0].Text, RelativeSource={RelativeSource AncestorType=Grid}}" Value="">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=Children[0].Text, RelativeSource={RelativeSource AncestorType=Grid}}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F5F5F5" Padding="10" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding SearchResultText}" VerticalAlignment="Center" FontWeight="Bold" FontSize="14"/>

                <Button Grid.Column="1" Content="リセット" Command="{Binding ResetCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="閉じる" Click="Close_Click" Width="80" IsCancel="True"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
            <StackPanel>

                <TextBlock Text="動画ファイルパス" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Path, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

                <TextBlock Text="動画時間 (hh:mm:ss / mm:ss / ss)" Style="{StaticResource LabelStyle}"/>
                <Grid Margin="0,5,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <TextBox Text="{Binding MinDuration, UpdateSourceTrigger=LostFocus}"/>
                        <TextBlock Text="例: 30, 10:00, 1:23:45">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MinDuration}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <TextBlock Grid.Column="1" Text=" ～ " VerticalAlignment="Center" Margin="5,0"/>

                    <Grid Grid.Column="2">
                        <TextBox Text="{Binding MaxDuration, UpdateSourceTrigger=LostFocus}"/>
                        <TextBlock Text="例: 30, 10:00, 1:23:45">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource PlaceholderTextStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding MaxDuration}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Grid>
                <TextBlock Text="{Binding DurationError}" Style="{StaticResource ErrorTextStyle}"/>

                <TextBlock Text="クリップ名" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding ClipName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

                <TextBlock Text="曲名" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding SongTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

                <TextBlock Text="公演名" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding ConcertName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

                <TextBlock Text="公演日 (例: 2025/01/01)" Style="{StaticResource LabelStyle}"/>
                <Grid Margin="0,5,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <DatePicker Grid.Column="0" SelectedDate="{Binding ConcertDateFrom, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text=" ～ " VerticalAlignment="Center" Margin="5,0"/>
                    <DatePicker Grid.Column="2" SelectedDate="{Binding ConcertDateTo, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                </Grid>
                <TextBlock Text="{Binding ConcertDateError}" Style="{StaticResource ErrorTextStyle}"/>

                <TextBlock Text="ライブ形式 - 複数選択可(OR)" Style="{StaticResource LabelStyle}"/>
                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5" Margin="0,5,0,15">
                    <ItemsControl ItemsSource="{Binding LiveTypeList}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Label}" IsChecked="{Binding IsSelected}" Margin="0,0,15,5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <TextBlock Text="ブランド - 複数選択可(OR)" Style="{StaticResource LabelStyle}"/>
                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5" Margin="0,5,0,15">
                    <ItemsControl ItemsSource="{Binding BrandList}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Label}" IsChecked="{Binding IsSelected}" Margin="0,0,15,5"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="出演者 - 複数選択可(OR)" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                    <Button Content="選択..." Command="{Binding SelectPerformersCommand}" Margin="10,0,0,0" Width="60"/>
                </StackPanel>

                <TextBox Text="{Binding PerformersText}" IsReadOnly="True" Background="#F0F0F0"
                         MinHeight="50" Height="Auto" MaxHeight="200"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         VerticalContentAlignment="Top"
                         Margin="0,0,0,15"/>

                <TextBlock Text="歌詞" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Lyrics, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

                <TextBlock Text="備考" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="登録日 (例: 2025/01/01)" Style="{StaticResource LabelStyle}"/>
                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <DatePicker Grid.Column="0" SelectedDate="{Binding CreatedFrom, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="～" VerticalAlignment="Center" Margin="2,0"/>
                            <DatePicker Grid.Column="2" SelectedDate="{Binding CreatedTo, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="{Binding CreatedDateError}" Style="{StaticResource ErrorTextStyle}" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="更新日 (例: 2025/01/01)" Style="{StaticResource LabelStyle}"/>
                        <Grid Margin="0,5,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <DatePicker Grid.Column="0" SelectedDate="{Binding UpdatedFrom, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="～" VerticalAlignment="Center" Margin="2,0"/>
                            <DatePicker Grid.Column="2" SelectedDate="{Binding UpdatedTo, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="{Binding UpdatedDateError}" Style="{StaticResource ErrorTextStyle}" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>