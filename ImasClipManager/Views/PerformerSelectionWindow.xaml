<Window x:Class="ImasClipManager.Views.PerformerSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="出演者選択" Height="500" Width="500" WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="表示設定・検索:" FontWeight="Bold" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="形式" FontSize="10" Foreground="Gray"/>
                    <ComboBox Width="100"
                              ItemsSource="{Binding LiveTypeList}"
                              SelectedItem="{Binding SelectedLiveTypeOption}"
                              DisplayMemberPath="Label"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="ブランド" FontSize="10" Foreground="Gray"/>
                    <ComboBox Width="120"
                              ItemsSource="{Binding BrandList}"
                              SelectedItem="{Binding SelectedBrandOption}"
                              DisplayMemberPath="Label" />
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="キーワード検索" FontSize="10" Foreground="Gray"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             VerticalContentAlignment="Center" Height="22"
                             ToolTip="名前、キャラ名、よみがなで検索"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <ListBox Grid.Row="1" ItemsSource="{Binding DisplayPerformers}" SelectionMode="Multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding DisplayName}" Margin="0,2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="キャンセル" Width="80" Margin="0,0,10,0" IsCancel="True"/>

            <Button Content="OK" Width="80" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>